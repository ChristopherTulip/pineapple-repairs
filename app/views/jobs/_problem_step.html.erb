<% @data.each do |problem| %>
  <div class="col-sm-3 col-xs-6 col-xs-offset-3 col-sm-offset-0 text-center text-center job-choice">
  	<%= image_tag("#{problem.name.downcase}.png") %>
    <%= f.button problem.name, name: "problem_id", value: problem.id, class: "btn btn-primary" %>
  </div>
<% end %>

